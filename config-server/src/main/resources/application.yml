spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Sergistan/config-repo.git
          search-paths: config-repo
server:
  port: 8071